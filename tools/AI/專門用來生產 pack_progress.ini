請只做「生成整包翻譯進度報表 pack_progress.csv」；嚴格禁止修改任何檔案（包含 tools/ 內任何腳本、package.json、.gitignore、README、resourcepacks 等），也禁止 git commit/push。

【固定環境】
RepoRoot: D:\stoneblock4-zh_tw
OutCsv:  D:\stoneblock4-zh_tw\pack_progress.csv

【執行要求】
1) 先確認目前 git 狀態必須是乾淨的：
   - 執行 git status --porcelain
   - 若輸出非空：停止並列出哪些檔案是 dirty（不要做任何修改）

2) 僅允許執行既有報表腳本（不得改檔）
   - 請在 RepoRoot 搜尋現有可用的報表入口（擇一執行）：
     優先順序：
     a) tools\calc-pack-progress.ps1
     b) tools\refresh_lang_reports.ps1
     c) package.json scripts 中與 progress/report 相關的命令

3) 執行後必須產出 OutCsv（pack_progress.csv）
   - 若沒有產出：停止並回報「你嘗試了哪些現有命令」與各自的錯誤訊息（不得修程式）

【輸出（console）】
- sb4-zh_tw overall progress: translated=__ / total=__, remaining=__, percent=__._%
- 表格前 30 行（modid translated total remaining percent）
- CSV 路徑：D:\stoneblock4-zh_tw\pack_progress.csv
- 任何讀取/解析錯誤（不中斷）

【重要限制】
- 不得新增檔案、不得修改檔案、不得安裝套件、不得更新依賴、不得格式化檔案
- 不得產生新的翻譯 bundle、不得套用翻譯、不得做 git commit/push
