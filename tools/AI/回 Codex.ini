I have finished the translation. Please continue the workflow ONCE for <MODID> using the fixed-branch + tools/_cache convention:

- Read translated bundle from: E:\stoneblock4-zh_tw\tools\_cache\<MODID>\bundle_translated.json
- Apply translations with tools\extract-zh-cn-to-zh-tw.ps1 -Overwrite (skip if missing)
- Generate the next keys bundle (max 200, allow <200)

Compatibility / namespace handoff rule (must follow, no loops):
- If the generated bundle has 0 keys, DO NOT stop immediately.
  Instead, perform a one-time namespace audit on the source jar:
  - Find the jar containing assets/<MODID>/lang/en_us.json (fallback en_us.lang) by zip entry check (do not guess by filename)
  - Enumerate all assets/*/lang/en_us.json (or .lang) namespaces in that jar
  - For up to 3 foreign namespaces (excluding <MODID>), generate handoff bundles:
    E:\stoneblock4-zh_tw\tools\out\_handoff\<MODID>\<NS>\bundle_to_translate.json (max 200 each)
    and corresponding prompt files in the same folder
  - Stop ONLY if <MODID> bundle is 0 AND all handoff bundles are also 0

- Run full duplicate check against existing resourcepacks zh_tw.json
  - For <MODID>: resourcepacks\sb4-zh_tw\assets\<MODID>\lang\zh_tw.json
  - If handoff bundles were generated, also run the same duplicate check for each <NS>:
    resourcepacks\sb4-zh_tw\assets\<NS>\lang\zh_tw.json
  - If duplicates exist, STOP and list the first 50 dup keys (no retries)

- Compute progress (before and after push)
  - For <MODID>, using jar entry assets/<MODID>/lang/en_us.json (fallback .lang)
  - If handoff namespaces exist, compute and print progress for each <NS> as well

- Commit/push only repo changes (do not commit tools/out, tools/_cache, or tools/out/_handoff)
- No loops, no new batch branches.
